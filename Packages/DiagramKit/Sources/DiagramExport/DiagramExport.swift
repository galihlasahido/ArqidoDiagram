/// Not implemented yet. Reserved for `ExportAdapter` implementations
/// (`PNGExportAdapter`, `SVGExportAdapter`, `PDFExportAdapter`) that reuse
/// `DiagramRendering`'s `ShapeGeometry` so exported output matches on-screen
/// rendering exactly, run off the main actor.
///
/// Lands at Phase 1 build-order step 20.
public enum DiagramExportModule {}
